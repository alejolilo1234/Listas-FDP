// Jhon Abril <jhon.abril@correounivalle.edu.co>

const p1 = { x: 1, y: 3 };
const p2 = { x: 4, y: 5 };

const coordenada2d = { d1: { x: p1.x, y: p1.y }, d2: { x: p2.x, y: p2.y } }

console.log(coordenada2d);